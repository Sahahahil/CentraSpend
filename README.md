To run and test the application on your local machine:

1. **Express Server**: Open a terminal, run `cd server && npm install && npm run dev`.
2. **React Frontend**: Open a second terminal, run `cd client && npm install && npm run dev` and navigate to the localhost port displayed in your terminal.
